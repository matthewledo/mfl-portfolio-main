---
const jobs = [
  {
    title: "Leon's Furniture Ltd.",
    role: "Digital Experience Developer",
    time: "May 2021 - Present",
    list: [
      "Own end-to-end front-end execution for an ecommerce site with 35.7M annual sessions[cite: 34].",
      "Sole developer for time-sensitive promotions like Black Friday and Boxing Day[cite: 36].",
      "Build JavaScript functionality for region-based pricing and dynamic UI interactions[cite: 38].",
      "Lead SEO execution contributing to 6.27M organic sessions annually[cite: 43]."
    ]
  },
  {
    title: "Holliston's Inc.",
    role: "E-Commerce Merchandiser",
    time: "2020 - 2021",
    list: [
      "Redesigned the ecommerce website and migrated 6,000+ products using SEO best practices[cite: 56].",
      "Built site navigation and collection hierarchies to improve UX[cite: 57].",
      "Managed WordPress and WooCommerce back-end operations[cite: 58]."
    ]
  }
];
---

<section class="max-w-6xl mx-auto px-6 py-20">
  <div class="flex items-center gap-4 mb-12">
    <h2 class="text-3xl font-bold text-slate-900 whitespace-nowrap">Professional Experience</h2>
    <div class="h-px bg-slate-200 w-full"></div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-4 gap-12">
    <div class="flex lg:flex-col overflow-x-auto lg:overflow-visible border-b lg:border-b-0 lg:border-l border-slate-200">
      {jobs.map((job, i) => (
        <button 
          class={`tab-btn px-6 py-4 text-left text-sm font-bold transition-all whitespace-nowrap border-l-2 lg:border-l-4 ${i === 0 ? 'border-emerald-500 text-emerald-600 bg-emerald-50/50' : 'border-transparent text-slate-400 hover:text-slate-600'}`}
          data-index={i}
        >
          {job.title}
        </button>
      ))}
    </div>

    <div class="lg:col-span-3">
      {jobs.map((job, i) => (
        <div class={`tab-content ${i === 0 ? 'block' : 'hidden'}`} data-index={i}>
          <h3 class="text-2xl font-bold text-slate-900">{job.role} <span class="text-emerald-600">@ {job.title}</span></h3>
          <p class="text-sm font-medium text-slate-500 mt-1 mb-6">{job.time}</p>
          <ul class="space-y-4">
            {job.list.map(item => (
              <li class="flex gap-3 text-slate-600 leading-relaxed">
                <span class="text-emerald-500 mt-1.5 font-bold">▹</span>
                {item}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>